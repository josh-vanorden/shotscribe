.build/
.swiftpm/
*.xcodeproj
DerivedData/
.DS_Store
dist/

# Local planning only — roadmap stays out of the repo (see Hook.md)
/roadmap.md

# /brag runs write their renders here — output, not source
brag-output*/
